# libsimg – Siemens Image Library

`libsimg` is a C library for decoding images from Siemens mobile phone fullflash dumps (SG, NSG, ELKA).

## Building
The SDK must be located at `../../sdk` relative to the project root.
```bash
mkdir build && cd build
cmake ..
make
```
